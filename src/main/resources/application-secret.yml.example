# MiniClaw 配置文件示例
#
# 如果使用生产环境，请复制此文件为 application-secret.yml
# 并填入真实的敏感信息
#
# 注意：application-secret.yml 不会被 Git 追踪（已在 .gitignore 中）

spring:
  datasource:
    # 生产环境数据库连接（示例）
    url: jdbc:postgresql://your-production-host:5432/miniclaw
    username: your-username
    password: your-password

# API Keys（如果有）
miniclaw:
  openai:
    api-key: sk-your-openai-api-key
  anthropic:
    api-key: sk-ant-your-anthropic-api-key
